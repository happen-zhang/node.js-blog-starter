/**
 * /config/blog_config.js
 */

// Blog配置
var config = {
  // 博客名称
  blogname: 'happen',
  // 分页行数
  listRows: 5,
  // 访问端口
  port: 3000,
  // session secret
  sessionSecret: '560b9047',
  // cookie max age 1 hour
  cookieMaxAge: 3600000
};

module.exports = config;
